# TradeEZ AI 工作入口

任何模型或自动化代理开始工作前，必须先确认当前事实，而不是依赖聊天记录或历史文档。

## 必读顺序

1. [当前状态](docs/00-项目状态/当前状态.md)
2. 当前 GitHub Issue 及其关联的 Epic、PR 和确认记录
3. [AI 接手与会话交接](docs/10-开发与协作/AI接手与会话交接.md)
4. [开发规则](docs/10-开发与协作/开发规则.md)
5. 当前模块在[模块目录](docs/20-产品与研究/模块目录.md)中指定的唯一规格

前端、数据库、连接器任务还必须读取对应专项规范。

## 开工检查

先报告并核对：仓库根目录、当前分支、HEAD、工作树、关联 Issue、任务阶段、允许修改目录、共享资源、端口、开发/测试/E2E 数据库和需要运行的门禁。发现事实与文档不一致时，以代码、schema、自动化测试和 GitHub 记录为准，并在任务中修正文档。

## 强制约束

- GitHub `main` 是合并代码事实源；业务改动必须经过 Issue、独立分支/worktree、PR、CI 和审查。
- 一个任务只允许一个主要编码者写入一个分支和 worktree；Codex 与其他编码代理不得同时修改同一分支。
- 先研究和评审，再做前端 Mock；产品负责人确认 Mock 后才能进入后端和真实数据联调。
- TradeZella 只作为结构、交互和业务流程研究对象；TradeEZ 使用自己的品牌、Token、组件、文案和产品规则。
- 测试使用独立 `tradeez_test_*`、`tradeez_e2e_*` 数据库；不得把开发、共享测试或生产库用于自动化测试。
- 数据库迁移、锁文件、导航、全局 Token 和共享契约是串行资源，修改前必须在 Issue 登记。
- 不提交 `.env`、密钥、数据库文件、真实认证状态、用户数据、运行日志和构建缓存。
- 未经当前 Issue 重新确认的旧设计不得作为开发授权；Notebook、笔记历史和笔记模板必须重新研究。
- 完成实现后更新模块规格的长期事实；动态进度只更新 GitHub，不在仓库复制任务流水。

## 完成要求

任务只有在范围和确认记录齐全、适用门禁通过、PR 审查完成、风险与回滚写明、数据库环境状态可追溯后才能合并。详细操作以 [GitHub 协作与交付流程](docs/10-开发与协作/GitHub协作与交付流程.md)为准。
