# TradeEZ 项目接手规则

开始任何工作前，依次阅读 `docs/00-项目总览/迁移与接手指南.md`、`docs/00-项目总览/开发交接指南.md`、`docs/00-项目总览/新工作启动操作手册.md`、`docs/00-项目总览/项目进度.md`、`docs/20-产品与需求/一期方向与大模块开发计划.md`、`docs/10-规则与规范/开发方法论.md`、`docs/10-规则与规范/全局样式规则.md`。模块设计文档以当前 Issue 指定版本为准，文档索引见 `docs/00-项目总览/项目文档索引.md`。

- 主干当前保留已提交的账户、交易记录、总览、策略和工程基础成果；下一期模块顺序以《一期方向与大模块开发计划》重新评审结果为准。
- 新业务模块独立重写，旧模块仅作兼容参考；验收后再按清单删除旧源码和接口。
- 先前端 mock，经确认后开发后端，再联调测试。交易账户模块已通过此前端门禁。
- 有参考产品时，必须按具体功能点逐个使用专用研究浏览器/CDP 实际观察参考产品的布局、间距、图标、字体、状态、弹层位置、hover/focus/loading/error 和交互反馈，再映射到 TradeEZ 自己的 UI Token 和标准控件。参考产品没有实现或无法观察时，不暂停开发，但必须遵循 TradeEZ UI 规范自主设计，并逐项登记到《前端自主设计清单》供用户评审。不得直接复制品牌、配色或文案。
- 当用户要求复刻参考页面时，必须执行 `docs/10-规则与规范/参考页面像素级复刻规则.md`。触发句为“按《参考页面像素级复刻规则》复刻：`<网址、页面名或功能点>`”，收到后不得凭记忆编码。
- 所有全局样式沿用已确认规则；中文和英文文案需要统一资源管理。
- 不提交环境变量、数据库密码、真实密钥、运行数据。数据库迁移先在隔离测试数据库验证，不自动对远程数据库执行破坏性操作。
- 保留 `AGENTS.md`、`README.md` 等工具入口名称；业务文档用中文名。
- 每次更新实现后更新进度；不得把 mock、入口占位或未执行的测试写成已完成后台功能。

## 多人协作与 Codex 工作模式

完整流程见 `docs/00-项目总览/开发机制总览.md`、`docs/10-规则与规范/Git分支与PR规范.md` 和 `docs/00-项目总览/开发交接指南.md`。新模块必须从 GitHub Issue 和模块开发卡片开始；Issue、PR、研究证据、Mock 确认和测试结果是交接事实源。

- GitHub `main` 是唯一事实源；所有改动经分支 + PR + 门禁合并，禁止直接推 `main`。
- 一个模块 = 一个分支 = 一个 Codex 任务；本地用 Git worktree 隔离，不同任务不得共写同一个工作目录。
- 每个任务开始前同步并 rebase 最新 `main`；合并前跑 `scripts/quality/check.sh`（后端 pytest + 前端 tsc/biome/build）。
- 模块只改自己归属目录；共享文件、模块归属与迁移串行申请见 `docs/00-项目总览/多人协作开发规范.md`。
- 数据库迁移强串行：`backend/app/migrations.py` 与 `backend/migrations/versions/` 同一时间只允许一个分支修改；新迁移必须同时更新 `_MIGRATION_STEPS` 与 `SCHEMA_VERSION`。
- 前端统一使用 npm 和 `package-lock.json`，不新增 `pnpm-lock.yaml`。
- 更新实现后同步更新 `docs/00-项目总览/项目进度.md` 和对应模块设计文档。
- Notebook、笔记历史和笔记模板旧方案已经归档，重新开发前不得引用归档文档作为需求或接口事实。
