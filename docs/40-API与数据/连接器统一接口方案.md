# 连接器统一接口方案

版本：0.1；状态：方案已确认，后端统一接口开发中。

## 1. 目标

首批平台优先级为 MT5，后续 MT4、cTrader、ATAS、CTP 均通过 API 接入。本文定义一套平台无关的连接器接口，使各平台只实现平台侧数据采集，不重复设计账户、成交、游标、幂等、重试和审计逻辑。

本方案不处理历史数据迁移，历史兼容单独决策。

## 2. 设计原则

1. 一个账户连接一个或多个连接器实例，连接器实例有稳定 `connection_id`。
2. 数据按类型化事件上报，服务端统一归一化。
3. 游标、批次、幂等、重试由统一协议承载。
4. 认证保持与现有同步密钥兼容，但增加连接器身份和协议版本。
5. 平台专用字段放入 `data` 和 `raw`，不在顶层制造平台分支。
6. 所有外部 ID 使用字符串，避免 MT5 数字账号、cTrader、CTP 投资者账号混用。

## 3. 连接器生命周期

```text
handshake
   -> fetch cursor
   -> collect platform events
   -> submit event batches
   -> commit cursor
   -> repeat
```

每个周期必须独立、可重试。事件提交与游标提交分离，游标只在实际提交成功后推进。

## 4. 认证

沿用同步密钥作为连接凭证：

```http
Authorization: Bearer <sync-key>
X-Protocol-Version: 1.0
X-Connector-Version: <connector-version>
X-Timestamp: <unix-seconds>
X-Signature: <hmac-sha256>
```

签名初版继续使用 `HMAC_SHA256(secret, body + timestamp)`，降低 MT4/MT5 EA 的加密实现成本。服务端可接受同一密钥访问属于该账户的所有连接器实例。

## 5. 连接器握手

`POST /api/v1/connectors/handshake`

```json
{
  "platform": "mt5",
  "account_ref": "921001",
  "instance_id": "mt5-terminal-01",
  "connector_version": "2.0.0"
}
```

响应：

```json
{
  "connection_id": "cn_01J...",
  "platform": "mt5",
  "protocol_version": "1.0",
  "capabilities": [
    "instruments",
    "trades",
    "orders",
    "positions",
    "snapshots",
    "heartbeat"
  ],
  "limits": {
    "max_batch_size": 1000,
    "max_events": 5000,
    "max_body_bytes": 4194304
  },
  "cursor": {
    "basis": "event_time",
    "value": 0,
    "state": {}
  },
  "upgrade": {
    "required": false,
    "required_version": "2.0.0",
    "download_url": "https://www.tradeez.cn/downloads/tradeezsync-v2.mq5",
    "message": ""
  }
}
```

当服务端要求升级时，`upgrade.required` 为 `true`。客户端必须在握手后立即检查该字段；需要升级时停止后续同步并展示下载入口。

## 6. 游标

`GET /api/v1/connections/{connection_id}/cursor`

```json
{
  "basis": "event_time",
  "value": 1789632000,
  "state": {
    "last_trade_id": "123",
    "last_batch_id": "batch-001"
  }
}
```

游标是平台无关的时间基准，平台可通过 `state` 保存连接器需要的最小状态。服务端不解析平台状态，只负责回传。

## 7. 事件批次

`POST /api/v1/connections/{connection_id}/events`

```json
{
  "batch_id": "batch-001",
  "batch_index": 0,
  "batch_count": 1,
  "idempotency_key": "batch-001",
  "events": [
    {
      "event_id": "trade:123",
      "type": "trade",
      "occurred_at": 1789632000,
      "data": {}
    }
  ]
}
```

响应：

```json
{
  "accepted": 1,
  "inserted": 1,
  "updated": 0,
  "duplicates": 0,
  "rejected": 0,
  "cursor": {
    "basis": "event_time",
    "value": 1789632000,
    "state": {}
  },
  "replay": false
}
```

服务端按 `(connection_id, event_id, type)` 幂等去重。批次重放返回相同结果，不重复入库。

## 8. 事件类型

### 8.1 品种

```json
{
  "event_id": "instrument:XAUUSD",
  "type": "instrument",
  "data": {
    "symbol": "XAUUSD",
    "exchange": null,
    "product": "XAUUSD",
    "contract": null,
    "price_tick": 0.01,
    "contract_multiplier": 100,
    "base_currency": "USD",
    "quote_currency": "USD",
    "raw": {}
  }
}
```

### 8.2 成交

```json
{
  "event_id": "trade:123",
  "type": "trade",
  "data": {
    "trade_id": "123",
    "position_id": "456",
    "order_id": "789",
    "symbol": "XAUUSD",
    "side": "buy",
    "direction": "long",
    "open_close": "close",
    "hedge_flag": "speculation",
    "quantity": 0.1,
    "price": 2050.5,
    "open_time": 1789600000,
    "close_time": 1789632000,
    "currency": "USD",
    "pnl": {
      "gross": 100,
      "fee": -2.5,
      "swap": 0,
      "net": 97.5
    },
    "raw": {}
  }
}
```

### 8.3 订单

```json
{
  "event_id": "order:789",
  "type": "order",
  "data": {
    "order_id": "789",
    "order_ref": "local-001",
    "system_order_id": "exchange-001",
    "symbol": "rb2510",
    "direction": "long",
    "open_close": "open",
    "hedge_flag": "speculation",
    "status": "filled",
    "quantity": 1,
    "price": 3200,
    "created_at": 1789632000,
    "raw": {}
  }
}
```

### 8.4 持仓

```json
{
  "event_id": "position:456",
  "type": "position",
  "data": {
    "position_id": "456",
    "symbol": "rb2510",
    "direction": "long",
    "quantity": 2,
    "open_quantity": 2,
    "close_quantity": 0,
    "average_open_price": 3190,
    "margin": 6380,
    "position_profit": 200,
    "raw": {}
  }
}
```

### 8.5 账户快照

```json
{
  "event_id": "snapshot:1789632000",
  "type": "account_snapshot",
  "data": {
    "balance": 10000,
    "equity": 10020,
    "margin": 100,
    "free_margin": 9920,
    "available": 9920,
    "frozen_margin": 0,
    "position_profit": 20,
    "close_profit": 0,
    "currency": "USD",
    "occurred_at": 1789632000,
    "raw": {}
  }
}
```

### 8.6 心跳

```json
{
  "event_id": "heartbeat:1789632000",
  "type": "heartbeat",
  "data": {
    "occurred_at": 1789632000,
    "connector_status": "online",
    "version": "2.0.0",
    "raw": {}
  }
}
```

## 9. 连接器适配层

每个平台实现一个连接器，只负责生成统一事件：

| 平台 | 连接器位置 | 数据来源 |
|---|---|---|
| MT5 | MT5 EA | 成交历史、品种、账户快照 |
| MT4 | MT4 EA | 成交历史、品种、账户快照 |
| cTrader | 服务端适配器 | Open API 订单、成交、持仓 |
| ATAS | 服务端适配器或插件 | API/导出订单与成交 |
| CTP | 服务端适配器 | CTAPI 订单、成交、持仓、资金 |

服务端统一执行鉴权、批次校验、幂等、归一化、指标重建和审计。

## 10. MT5 全新改造范围

现有 `tradeEZSync.mq5` 将按统一协议重写：

- 增加握手和连接器会话模块。
- 使用结构化 JSON 解析与序列化，不再手工截取字符串。
- 成交、品种、快照、心跳统一进入 `events` 批次。
- 保留租约、调度、手动同步、错误重试和面板状态。
- 仅读取当前连接所需数据，不处理历史迁移。
- 认证头和签名逻辑抽取为独立模块。

## 11. 错误与重试

错误统一返回：

```json
{
  "code": "BATCH_CONTENT_CONFLICT",
  "message": "batch content conflict",
  "retryable": false,
  "details": {}
}
```

关键错误码：

- `INVALID_SIGNATURE`
- `CONNECTION_NOT_FOUND`
- `BATCH_ID_CONFLICT`
- `BATCH_CONTENT_CONFLICT`
- `BATCH_INDEX_OUT_OF_RANGE`
- `CURSOR_REGRESSION`
- `UNSUPPORTED_EVENT_TYPE`
- `RATE_LIMITED`

网络错误、5xx、`RATE_LIMITED` 可重试；签名、账户不存在、内容冲突等业务错误不重试。

## 12. 兼容策略

现有 `/api/v1/sync/*` 和 `/api/v1/ingest/*` 作为 MT5 旧接口暂时保留。新 MT5 连接器接入统一接口并验收后，再评估旧接口下线。

## 13. 验收重点

- 新 MT5 EA 能完成握手、游标、成交批次、品种、快照和心跳。
- 事件批次幂等，重放不产生重复交易。
- 服务端能接受不同平台的统一事件并生成相同交易结构。
- MT4、cTrader、ATAS、CTP 后续接入时无需修改账户、交易和总览核心。
- 平台原始数据保留在 `raw`，可追溯来源。

## 14. 当前实现入口

- 后端路由：`backend/app/connectors/router.py`
- 协议模型：`backend/app/connectors/schemas.py`
- 会话与事件服务：`backend/app/connectors/service.py`
- 持久化：`backend/app/connectors/repository.py`
- 认证：`backend/app/connectors/auth.py`
- 迁移：`backend/migrations/versions/0030_connector_unified.sql`
- 测试：`backend/tests/test_connectors.py`
- MT5 客户端第一版：`connectors/mt5/sync/v2/tradeEZSyncV2.mq5`

## 15. 客户端升级机制

- 服务端通过配置 `connector_mt5_min_version` 和 `connector_mt5_download_url` 管理 MT5 最低版本。
- 客户端握手时上报 `connector_version`。
- 服务端比较版本后返回 `upgrade` 对象。
- 客户端版本过低时：
  - 设置“需要升级”状态。
  - 展示升级提示和下载地址。
  - 显示“下载最新连接器”按钮。
  - 点击按钮调用系统浏览器打开下载页。
- 未要求升级时隐藏下载按钮，继续正常同步。
