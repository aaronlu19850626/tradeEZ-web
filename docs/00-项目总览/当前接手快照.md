# TradeEZ 当前接手快照

版本：1.0；状态：当前有效（2026-09-24）；用途：让新模型或新成员在不依赖聊天记录的情况下恢复当前项目状态；维护人：项目负责人；依赖：`main`、项目进度、一期方向与大模块开发计划；验收入口：核对 Git、CI、运行状态和下一步唯一动作。

## 1. 接手结论

- 实际仓库根目录由 `git rev-parse --show-toplevel` 确认；当前这台电脑为 `/Users/aaron/Documents/pro/tradeEZ-web/github-src`。
- GitHub `main` 是代码事实源。已验证的业务与治理基线为 `e0fa2ee6d7040e9f0840bcf1706e1dfb5a321013`；后续纯文档提交可能使 HEAD 前进，接手时必须重新核对本地 `main`、`origin/main` 和工作树。
- 一次性基线治理已经完成。此前未提交的业务代码已保留并纳入主干；本轮例外结束，后续工作恢复 Issue、独立分支/worktree、PR 和 CI 流程。
- 基线远端 CI 已通过：后端、前端和 Playwright 浏览器回归全部成功。运行记录：<https://github.com/aaronlu19850626/tradeEZ-web/actions/runs/35978680725>。
- 当前尚未确认一期模块范围和顺序。用户已明确决定：先全面盘点 TradeZella 核心模块，再共同确定优先级。
- Notebook、笔记历史和笔记模板旧方案已经归档，不是当前需求。不得从归档恢复排期、接口或验收结论。

## 2. 当前代码与验收边界

| 区域 | 可核查事实 | 接手时不得推断 |
|---|---|---|
| 账户、交易记录、交易总览 | 已有前后端实现并通过基线回归 | 不等于已按新流程确认一期范围或最终产品验收 |
| 策略与 Playbooks | 存在前端实现 | 不等于已确认进入一期 |
| 通用 Lexical 编辑器 | 保留在设计系统作为实验资产 | 不等于新 Notebook 已选定编辑器 |
| Notebook、笔记历史、笔记模板 | 活跃需求和旧入口已重置，旧材料在历史归档 | 不得继续旧方案或把历史截图当新需求 |
| 工程治理 | Issue/PR 模板、CODEOWNERS、独立数据库门禁、CI 和交接规范已建立 | 不代表真实业务模块已经完成新流程演练 |

历史模块文档只说明当时实现或设计。引用前必须核对当前代码、当前 Issue 和本表；文档中的“完成”不能自动升级为当前一期承诺。

## 3. 已验证的基线

- 后端：96 项测试通过。
- 前端：TypeScript、Biome、Webpack 生产构建通过。
- 浏览器：Playwright 70 项通过，覆盖桌面、平板和移动端。
- 远端：GitHub CI 三个 Job 全部通过。
- 包管理：前端统一 npm 与 `package-lock.json`，不得新增 `pnpm-lock.yaml`。
- 测试数据：pytest 和 E2E 必须分别使用 `tradeez_test_*`、`tradeez_e2e_*` 独立数据库。

测试结果只证明上述基线提交。新改动必须重新运行与范围相符的门禁。

## 4. 当前本机运行提示

交接时开发服务使用前端 `127.0.0.1:3000`、后端 `127.0.0.1:8000`。本地 `frontend/.env.local` 曾残留旧局域网地址并导致验证码页面显示 `Failed to fetch`，现已改为：

```dotenv
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000/api/v1
```

该文件被 Git 忽略，不属于主干。新模型应先检查端口和健康状态，不能假设进程仍在运行，也不能输出 `.env` 的其他值。开发测试模式固定验证码为 `123456`；生产环境不得启用测试验证码。若要从另一台设备通过局域网访问，需另行配置可达的后端地址和 CORS，不能继续使用客户端自己的 `127.0.0.1`。

## 5. 当前未决项

1. TradeZella 核心模块地图、用户路径、功能覆盖矩阵和跨模块数据关系尚未研究完成。
2. TradeEZ 现有代码与 TradeZella 能力的差距矩阵尚未形成。
3. 一期范围、优先级、开发波次、验收深度和明确不做项尚未由用户确认。
4. Notebook 是否进入一期、采用何种信息架构和编辑器均未决定。
5. 新协作流程尚未用一个真实业务模块完整演练。
6. 非阻塞技术遗留包括部分 Recharts 隐藏容器尺寸警告、总览冷路径性能，以及 GitHub Actions 运行环境未来迁移提示。

## 6. 下一步唯一动作

创建“TradeZella 核心模块全面盘点”研究 Issue，使用《TradeZella 核心模块盘点模板》逐模块采集证据。研究阶段只产出事实、能力地图、数据关系和现状差距，不开始 Notebook 或其他业务编码。所有模块盘点完成后，再与用户进行一期优先级评审。

接手模型的首条回复应先报告：实际仓库根目录、分支、HEAD、工作树、当前运行状态、已读入口、当前阶段和第一项动作。若实际 Git 或运行状态与本快照不同，以可核查事实为准并更新本文件。

## 7. 必读顺序

1. 根目录 `AGENTS.md`。
2. 本快照。
3. 《项目进度》与《一期方向与大模块开发计划》。
4. 《新工作启动操作手册》与《开发交接指南》。
5. 《项目文档索引》。
6. 当前 Issue 指定的模块文档；没有 Issue 时不得自行选择业务模块开工。
