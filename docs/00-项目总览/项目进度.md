# TradeEZ 项目进度

版本：3.0；状态：本地门禁已通过，待提交与远端 CI；用途：记录当前可执行状态、未决项和下一步；维护人：项目负责人；依赖：开发机制总览、一期方向与大模块开发计划；验收入口：主干基线提交与 CI。

## 当前目标

1. 完整保留此前未提交的代码成果并纳入新的主干基线。
2. 将 Notebook、笔记历史和笔记模板旧方案移出当前事实源。
3. 统一 npm、隔离数据库、静态门禁、Playwright 和 GitHub CI。
4. 主干干净且可运行后，全面盘点 TradeZella 核心模块。
5. 盘点完成后，由用户与技术负责人共同确认一期大模块、顺序和验收深度。

## 当前代码状态

| 区域 | 状态 | 说明 |
|---|---|---|
| 账户、交易记录、交易总览 | 已有前后端实现，纳入本轮基线验证 | 最终产品状态以后续盘点和用户验收为准 |
| 策略与 Playbooks | 已有前端代码，纳入本轮基线验证 | 是否进入一期尚未决定 |
| 通用 Lexical 编辑器 | 保留为共享实验资产 | 不代表新 Notebook 的编辑器选型或需求已经确认 |
| Notebook、笔记历史、笔记模板 | 旧方案已归档，当前无开发授权 | 必须重新研究、评审和做前端 Mock |
| 工程治理 | 文档、模板、CI 和隔离门禁收口中 | 本轮基线通过后生效 |

## 本轮验证记录

- 后端使用显式 `tradeez_test_*` 隔离数据库，不再隐式读取开发环境数据库。
- 浏览器回归使用空的 `tradeez_e2e_*` 隔离数据库和合成测试用户。
- 前端统一使用 npm 与 `package-lock.json`，删除 `pnpm-lock.yaml`。
- 本地静态门禁：后端 `96 passed`；TypeScript、Biome 和 Webpack 生产构建通过。
- 本地浏览器门禁：Playwright `70 passed`，覆盖桌面、平板和移动端。
- 提交 SHA 与远端 CI 状态在基线提交和推送后补录。

## 当前未决项

- TradeZella 核心模块地图、功能覆盖矩阵和跨模块数据关系尚待全面研究。
- 一期范围、模块优先级、开发波次和明确不做项尚待用户评审。
- 旧模块设计文档可能包含历史假设；新任务只能引用当前 Issue 指定的文档版本。

## 下一步

1. 完成本轮全仓库静态检查、生产构建和 Playwright 回归。
2. 提交并推送一次性主干基线，确认工作树干净和 CI 状态。
3. 创建“TradeZella 核心模块盘点”研究 Issue，按统一模板逐模块采集证据。
4. 汇总 TradeZella 能力与 TradeEZ 现有代码差距。
5. 与用户开展一期优先级评审，形成正式路线图后再创建业务开发任务。

## 历史记录

旧进度总表保存在 `docs/90-历史归档/旧进度总表-20260924.md`，只用于追溯，不代表当前验收状态、主题规则或开发顺序。
