---
doc_id: PROJECT-STATUS
status: active
owner: project-owner
last_verified: 2026-09-24
---

# 当前状态

## 主干基线

- 最近确认的远程 `main` 基线：`e0fa2ee6d7040e9f0840bcf1706e1dfb5a321013`。
- 基线包含认证、账户、交易记录、交易总览、MT5 同步、CI 和隔离测试门禁。
- 基线 CI 的后端、前端和 Playwright Job 已通过；后续提交必须以各自 PR 的新结果为准。
- 前端统一使用 npm 和 `package-lock.json`。

## 当前阶段

项目处于“TradeZella 核心模块全面盘点和一期重新评审”阶段。动态任务、负责人和进度由 GitHub Project 管理，本文件不复制看板流水。

## 模块边界

- 账户、交易记录、交易总览、分析、复盘和每日计划存在不同程度的实现，但仍需按新流程复核，不能自动视为一期验收完成。
- 策略与 Playbook 前端 Mock 暂停，恢复必须由新的 Issue 明确授权。
- Notebook、笔记历史和笔记模板没有当前有效需求，必须重新研究和评审。
- 旧文档已从当前文档体系移除，不能从 Git 历史中的旧稿恢复开发排期。

## 当前未决项

1. TradeZella 核心模块地图、用户路径、功能覆盖和跨模块数据关系。
2. TradeEZ 现有能力的已实现、部分实现、Mock、待重写和缺失差距矩阵。
3. 一期范围、优先级、开发波次和明确不做项。
4. 生产测试环境的 GitHub Environment、迁移审批和发布流水线实际配置。

## 当前下一阶段出口

完成核心模块研究 Issue 和差距矩阵，经产品负责人确认一期路线图后，才创建首个业务模块开发 Issue。交接者必须重新核对 Git 分支、HEAD、工作树、运行服务和 GitHub 状态，不得假定本文记录仍代表实时状态。
