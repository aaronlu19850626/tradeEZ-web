# Notebook 旧进度记录

以下记录已从当前进度台账移出，仅用于追溯，不作为新开发依据。

| 2026-09-23 | 调整产品模块开发顺序：订单明细前先完成交易策略与 Playbook，再完成笔记本模块；订单明细只复用稳定契约。新增笔记本模块设计和策略模块一期边界，明确现有数据库表不等于已交付业务能力 | 已确认 |
| 2026-09-23 | 完成 Markdown 可视化编辑器选型：对比 MDXEditor、Novel、Milkdown、Tiptap、Lexical、BlockNote 和 Plate，推荐采用 MIT 许可、Markdown 优先且支持图片/表格/源码模式的 `@mdxeditor/editor`；Notebook 与 Strategy Notes 将复用同一编辑器组件 | 待确认 |
| 2026-09-23 | 通过专用研究浏览器完成 Notebook 全量需求盘点：覆盖文件夹、收藏、交易笔记、每日日志、会话回顾、自定义文件夹、笔记搜索/筛选、Markdown 编辑器、模板、标签、自动保存、导入导出、分享、版本历史、回收站和笔记操作；输出 P0/P1/P2 清单、数据模型调整和专业评审建议 | 待评审 |
| 2026-09-23 | 用户确认 Notebook 一期范围、数据关系、安全生命周期和开发门禁；交易策略模块暂缓，Notebook 优先；历史版本提升为一期 P0；编辑器保留 Tiptap、Plate、BlockNote、MDXEditor 四个候选等待选型 | 已确认 |
| 2026-09-23 | 用户确认 Notebook 编辑器采用 Plate，许可证 MIT，当前安装版本 `platejs 53.3.14`；后续 AI、语音输入、版本历史和自定义节点通过 Plate 插件体系扩展 | 已确认 |
| 2026-09-23 | 完成 Notebook 前端 Mock N1：新增 `/dashboard/notebook`，实现三栏布局、文件夹、笔记列表、Markdown 编辑器、自动保存、收藏、标签、模板、历史版本、回收站和恢复；Plate 解析/序列化、TypeScript、Biome 和 Webpack 构建通过，自主设计项已登记待评审 | 待验收 |
| 2026-09-23 | 扩展 Notebook Mock N1：新增编辑器块级格式下拉、列表/待办/引用/代码块/分割线/链接按钮，自定义文件夹重命名与删除，模板管理弹层，笔记列表分批展开，编辑器全宽模式和回收站永久删除；补充研究浏览器编辑器头部与格式菜单观察截图，TypeScript、Biome 与 Webpack 构建通过 | 待验收 |
| 2026-09-23 | 重新选型 Notebook 编辑器为 Tiptap v3：替换 Plate，新增 `@tiptap/starter-kit`、`@tiptap/markdown`、表格、图片、待办、对齐、高亮、Placeholder、Bubble Menu 和 Suggestion；实现划词快捷格式菜单与 `/` 命令菜单，保持 Markdown 字符串存储；TypeScript、Biome 与 Webpack 生产构建通过 | 待验收 |
| 2026-09-24 | 修复 Notebook 图片缩放与裁剪：统一节点宽高为未裁切基准尺寸，裁切比例独立保存，解决先裁剪再缩放时的二次裁切和图片细条问题；缩放改用方向/对角图标，裁切改用按边旋转的剪刀图标并放到边缘 75% 区域；图片单击进入编辑态，点击外部或按 `Esc` 退出；关闭编辑器右键菜单，新增右键、连续裁切、裁切后缩放和编辑态回归 | 已完成 |
| 2026-09-24 | 规范化全局页面布局与编辑器工程：新增《全局页面布局与编辑器开发总结》和编辑器目录 README；模块页面布局约束升级为已生效版本；确认 Lexical 为唯一编辑器实现，删除未使用的 Tiptap 编辑器源码并移除相关依赖；同步更新项目文档索引、Notebook 设计和进度台账 | 已完成 |
| 2026-09-24 | 完成 Notebook 模板管理与历史版本前端 Mock：模板拆为系统/用户模板、搜索筛选、复制、编辑、默认和删除，支持按笔记范围选择；历史版本实现手动/自动/离开/恢复快照、预览、行级差异和恢复留痕；两条功能线分别新增 Playwright 用例，Notebook 8 项回归、TypeScript、Biome 和生产构建通过，进入用户评审门禁 | 待评审 |
| 2026-09-24 | 固化可复用编辑器组件：将 Notebook 编辑器从业务模块解耦并重命名为通用 `LexicalEditor`，模板类型下沉为编辑器内 `EditorTemplate`，移除对 `notebook/_lib/template-model` 的引用；设计系统编辑器样例改为复用该通用组件，删除重复的 demo 编辑器实现；TypeScript 与 Webpack 生产构建通过，待用户评审 | 待评审 |
| 2026-09-24 | 抽取笔记通用模块：新增 `components/notebook`，将模板管理、历史版本、版本 diff 和对应 hook 从 Notebook 页面独立出来，模板持久化通过 `TemplateStore` 注入、历史弹窗只消费版本数据；Notebook 页面改为薄适配层复用通用组件，删除原 notebook 下重复的模板/历史组件目录；TypeScript、Biome 与 Webpack 生产构建通过，待用户评审 | 待评审 |
| 2026-09-24 | 清空 Notebook 前端 Mock 并重建开发计划：删除 `/dashboard/notebook` 路由、侧边栏入口、Notebook 测试和 Mock 评审文档；通过已登录研究浏览器抓取 Notebook 三栏布局、编辑器工具栏、模板/标签/历史弹层，形成《笔记本模块开发计划》待审批 | 待审批 |
